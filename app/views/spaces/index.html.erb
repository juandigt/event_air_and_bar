<div class="container">
  <div class="row filter-bar">
    <div class="col-xs-12">
      <div class="form-inline">
        <%= simple_form_for(spaces_path, method: :get) do %>
          <%= text_field_tag :address, params[:address], placeholder: "Type in a city", class: "form-control form-fields" %>
          <%= text_field_tag :price, params[:price], placeholder: "Price", class: "form-control form-fields" %>
          <%= text_field_tag :capacity, params[:capacity], placeholder: "Capacity", class: "form-control form-fields" %>
          <%= submit_tag 'Search', class: "btn btn-primary form-control" %>
        <% end %>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-xs-12">
      <h1>Choose venue for your next event</h1>
    </div>
  </div>
  <div class="row">
    <% @spaces.each do |space| %>
      <div class="col-xs-12 col-md-6 col-lg-4">
        <div class="card" style="background-image: linear-gradient(rgba(0,0,0,0.8), rgba(0,0,0,0.6)), url('https://picsum.photos/900/600/?random');">
          <div class="card-category">
            <%= space.name %>
          </div>
          <div class="card-description">
            <h2><%= space.address %></h2>
            <p>User id<%= space.user_id %></p>
            <!-- <%= link_to "view details >" , space_path(space), class: "btn btn-info" %> -->
            <!-- # <%= link_to "Delete " , space_path(space), method: :delete %> -->
          </div>
          <%= link_to "view details >" , space_path(space), class: "card-link" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
